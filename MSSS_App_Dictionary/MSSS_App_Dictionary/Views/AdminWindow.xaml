<Window x:Class="MSSS_App_Dictionary.Views.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MSSS_App_Dictionary.Views"
        mc:Ignorable="d"
        Title="Admin Functions"
        Height="350"
        Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown"
        Closing="Window_Closing">

    <Border BorderBrush="Gray" BorderThickness="2">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Staff Record Administration" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Staff ID:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtStaffId" Grid.Column="1" Margin="5" IsReadOnly="True" Background="#EEEEEE"/>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Staff Name:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtStaffName" Grid.Column="1" Margin="5"/>
            </Grid>

            <TextBlock Grid.Row="3" x:Name="lblInstructions" TextWrapping="Wrap" Margin="0,20,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" FontSize="14" LineHeight="22">
            </TextBlock>

            <StatusBar Grid.Row="4">
                <StatusBarItem>
                    <TextBlock x:Name="lblStatus" Text="Window loaded."/>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Border>
</Window>